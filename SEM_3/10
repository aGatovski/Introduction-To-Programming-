

#include <iostream>

int main()
{
    int a, b,sum=0;
    std::cin >>a >> b;
    for (int i = a; i < b; i++)
    {
        for (int j = 1; j < i; j++)
        {
            if (i % j == 0)sum +=j;
        }
        if (sum == i) { std::cout << sum << std::endl; sum = 0; }
        else sum = 0;
    }
   
    
}
