void foundIn( int arr20[])
{
	for (int i = 0; i <= 20; i++)
	{
		int c = 0;
		for (int j = 0; j < 20; j++)
		{
			if (i == arr20[j])c++;
		}
		std::cout << i << " is found " << c << " number of times" << std::endl;
	}
}
