#include <iostream>
using namespace std;
const int MAX = 100;

int main() {

	int matrix[MAX][MAX];
	int length;
	cin >> length;
	for (int i = 0; i < length; i++)
	{
		for (int j = 0; j < length; j++)
		{
			int a;
			cin >> a;
			matrix[i][j] = a;
		}
	}


	cout << "Diagonal 1" << endl;
	for (int i = 0; i < length; ++i) {
		std::cout << matrix[i][i] << " ";
	}
		std::cout << std::endl;

	cout << "Diagonal 2" << endl;

	for (int i = 0; i < length; ++i) {
		std::cout << matrix[i][length - 1 - i] << " ";
	}
	
	
}
